﻿describe('Account screen', function() { 
    describe('when user has to log in', function() {
        // TODO (made by AA).
    });

    describe('Account screen when user logged in)', function() {
        // BW - SLA1 - BuyCo POC - Seller Side
        it('should show a seller-details button with a text like "Sign up as a seller" when the user is not a seller');
        it('should NOT show "Sign up as a seller" text but something like "Edit seller info" instead, when the user is a seller.');
        it('should go the "sign-up-as-seller/edit seller info" screen when the user clicks the seller-details button');
    });
});